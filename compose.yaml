services:
  app:
    build: .
    working_dir: /workspace
    volumes:
      - .:/workspace
    network_mode: none
    gpus: all
    environment:
      PADDLEOCR_DEVICE: gpu:0
    command: ["python", "-m", "app.cli", "--help"]
